mutation CreateAssessmentAttempt(
  $courseId: ID!
  $questions: [QuestionInput!]!
  $topicId: ID!
) {
  CreateAssessmentAttempt(
    courseId: $courseId
    questions: $questions
    topicId: $topicId
  ) {
    id
  }
}
